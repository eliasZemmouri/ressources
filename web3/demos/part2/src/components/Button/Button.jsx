const Button = (props) => {
    
    return(
        <div>
            <button onClick={ () => console.log()} >
                hel
            </button>
        </div>
    )
}


export default Button;